# See: https://lefthook.dev/configuration/

pre-push:
  jobs:
    - name: mise run check
      run: mise run check

pre-commit:
  jobs:
    - name: mise run check
      run: mise run check

    - name: gitleaks protect
      run: mise run gitleaks-protect
